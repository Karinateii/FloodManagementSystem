@model IEnumerable<GlobalDisasterManagement.Models.City>

@{
    ViewData["Title"] = "City Management";
}

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h2 class="mb-0">
                        <i class="fas fa-city text-warning me-2"></i>City Management
                    </h2>
                    <p class="text-muted mb-0">Manage cities and their associated regions</p>
                </div>
                <div>
                    <a asp-action="CreateCity" class="btn btn-warning text-white me-2">
                        <i class="fas fa-plus me-1"></i> Create New City
                    </a>
                    <a asp-controller="Admin" asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-1"></i> Back
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th><i class="fas fa-city me-2"></i>@Html.DisplayNameFor(model => model.Name)</th>
                            <th><i class="fas fa-map-marked-alt me-2"></i>@Html.DisplayNameFor(model => model.LGA.LGAName)</th>
                            <th class="text-center" style="width: 250px;">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="table-secondary">
                            <td><strong>None</strong></td>
                            <td><strong>None</strong></td>
                            <td class="text-center">
                                <span class="badge bg-secondary">System Default</span>
                            </td>
                        </tr>
                        @if (Model?.Any() == true)
                        {
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="align-middle">
                                        <strong>@Html.DisplayFor(modelItem => item.Name)</strong>
                                    </td>
                                    <td class="align-middle">@Html.DisplayFor(modelItem => item.LGA.LGAName)</td>
                                    <td class="text-center align-middle">
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                                <i class="fas fa-edit"></i> Edit
                                            </a>
                                            <a asp-action="Detail" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">
                                                <i class="fas fa-info-circle"></i> Details
                                            </a>
                                            <a asp-action="DeleteCity" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger"
                                               onclick="return confirm('Are you sure you want to delete this city?');">
                                                <i class="fas fa-trash"></i> Delete
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="3" class="text-center py-5 text-muted">
                                    <i class="fas fa-city fa-3x mb-3 opacity-25"></i>
                                    <p class="mb-0">No cities found. Click "Create New City" to add one.</p>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
